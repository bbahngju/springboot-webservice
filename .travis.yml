language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# gradlew 실행 접근 권한
before_install:
  - chmod +x gradlew

# Travis CI 서버의 HOME
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - qkdwn0313@gmail.com